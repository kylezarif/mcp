[project]
name = "sec-filings"
version = "0.1.0"
description = "MCP server for SEC EDGAR company submissions and facts (no API key required)."
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.21.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
