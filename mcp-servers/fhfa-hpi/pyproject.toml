[project]
name = "fhfa-hpi"
version = "0.1.0"
description = "MCP server for FHFA House Price Index CSV datasets (no key required)."
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.21.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
